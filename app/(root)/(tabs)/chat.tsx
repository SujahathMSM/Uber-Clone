import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Chat = () => {
  return (
    <SafeAreaView>
      <Text>Chat-Sujahath</Text>
    </SafeAreaView>
  );
};

export default Chat;
