import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native";

const Rides = () => {
  return (
    <SafeAreaView>
      <Text>Rides-Sujahath</Text>
    </SafeAreaView>
  );
};

export default Rides;
